name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-macos-universal:
    name: Build macOS Universal Binary
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: "1.26"

      - name: Build arm64
        env:
          GOOS: darwin
          GOARCH: arm64
          CGO_ENABLED: "0"
        run: go build -trimpath -ldflags="-s -w -X main.version=${{ github.ref_name }}" -o maints-arm64 ./cmd/maints

      - name: Build amd64
        env:
          GOOS: darwin
          GOARCH: amd64
          CGO_ENABLED: "0"
        run: go build -trimpath -ldflags="-s -w -X main.version=${{ github.ref_name }}" -o maints-amd64 ./cmd/maints

      - name: Create universal binary
        run: lipo -create -output maints maints-arm64 maints-amd64

      - name: Verify universal binary
        run: lipo -info maints

      - name: Create archive
        run: |
          tar -czf maints-${{ github.ref_name }}-darwin-universal.tar.gz maints
          shasum -a 256 maints-${{ github.ref_name }}-darwin-universal.tar.gz > maints-${{ github.ref_name }}-darwin-universal.tar.gz.sha256

      - name: Create draft release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          generate_release_notes: true
          files: |
            maints-${{ github.ref_name }}-darwin-universal.tar.gz
            maints-${{ github.ref_name }}-darwin-universal.tar.gz.sha256
